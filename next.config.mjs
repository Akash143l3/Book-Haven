/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.w3schools.com", "anotherdomain.com", "yetanother.com"], // Add more domains as needed
  },
};

export default nextConfig;
